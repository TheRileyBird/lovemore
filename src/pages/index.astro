---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import StepCard from '../components/StepCard.astro';
import PillarCard from '../components/PillarCard.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
import Divider from '../components/Divider.astro';
import { Heart, Calendar, Users, Sparkles, Mail, ArrowRight } from 'lucide-astro';

const steps = [
	{ number: 1, title: 'Buy Your 3 Week Intro Pass', description: 'Purchase your intro pass online or in person' },
	{ number: 2, title: 'Show Up 15 Minutes Before Class Starts', description: 'Arrive early to get settled and meet your instructors' },
	{ number: 3, title: 'Feel The Love & Try Your Best', description: 'Connect with our welcoming community and begin your transformation' }
];

const pillars = [
	{
		icon: Heart,
		title: 'Movement',
		description: 'Meet us on the mat to unlock your inner strength and zen. Embark on the personal journey of connecting your mind, body, and spirit through movement.',
		link: '/movement',
		image: 'https://images.unsplash.com/photo-1544367567-0f2fcb009e0b?w=800&h=1000&fit=crop&q=80'
	},
	{
		icon: Users,
		title: 'Community',
		description: 'Here is where play and unity come alive! Gather, as we share and explore classes, events, retreats, and trainings.',
		link: '/community',
		image: 'https://images.unsplash.com/photo-1545389336-cf090694435e?w=800&h=1000&fit=crop&q=80'
	},
	{
		icon: Sparkles,
		title: 'Connection',
		description: 'Find home in our healing services, where a range of offerings awaits to bring harmony and rejuvenation to both your physical and spiritual selves.',
		link: '/connection',
		image: 'https://images.unsplash.com/photo-1506126613408-eca07ce68773?w=800&h=1000&fit=crop&q=80'
	}
];
---

<Layout>
	<Navigation />

	<main id="main-content">
		<!-- Hero Section -->
		<section class="relative min-h-[85vh] md:min-h-screen flex items-center justify-center overflow-hidden pt-20" aria-labelledby="hero-heading">
			<div class="absolute inset-0 z-0 bg-neutral-900">
				<!-- Fallback image -->
				<img src="https://images.unsplash.com/photo-1588286840104-8957b019727f?w=1600&h=1200&fit=crop&q=80" alt="" role="presentation" loading="eager" class="absolute inset-0 w-full h-full object-cover" />
				<!-- Vimeo video background -->
				<iframe
					src="https://player.vimeo.com/video/906709028?h=238bfbb4ec&autoplay=1&loop=1&muted=1&background=1&controls=0"
					class="absolute inset-0 w-full h-full pointer-events-none z-10"
					style="position: absolute; top: 50%; left: 50%; width: 100vw; height: 56.25vw; min-height: 100vh; min-width: 177.77vh; transform: translate(-50%, -50%);"
					frameborder="0"
					allow="autoplay; fullscreen; picture-in-picture"
					title="Hero Background Video"
				></iframe>
				<!-- Dark overlay -->
				<div class="absolute inset-0 bg-gradient-to-b from-neutral-900/50 via-neutral-900/40 to-neutral-900/55 z-20"></div>
			</div>

			<div class="relative z-10 container mx-auto px-6 text-center">
				<div
					x-data="{
						phrases: ['Love the Journey', 'Trust the Process', 'Give Gratitude', 'Receive More'],
						current: 0,
						opacity: 1,
						interval: null,
						init() {
							this.interval = setInterval(() => {
								this.opacity = 0;
								setTimeout(() => {
									this.current = (this.current + 1) % this.phrases.length;
									this.opacity = 1;
								}, 1200);
							}, 5000);
						},
						destroy() { if (this.interval) clearInterval(this.interval); }
					}"
					@destroy.window="destroy()"
				>
					<h1
						x-text="phrases[current]"
						:style="`opacity: ${opacity}; transition: opacity 1200ms ease-in-out;`"
						id="hero-heading"
						class="text-6xl md:text-8xl font-bold tracking-wide leading-tight animated-gradient-text"
					>Love the Journey</h1>
				</div>

				<p class="text-xl md:text-2xl text-white/95 mb-16 max-w-3xl mx-auto font-light leading-relaxed tracking-wide" style="text-shadow: 0 0 30px rgba(0,0,0,0.85), 0 2px 20px rgba(0,0,0,0.9), 0 4px 40px rgba(0,0,0,0.6);">
					movement <span class="text-gold">•</span> community <span class="text-gold">•</span> connection
				</p>

				<Button href="#get-started" variant="primary" size="large">
					Begin Your Journey — $56
				</Button>
			</div>
		</section>

		<!-- Getting Started Section -->
		<section id="get-started" class="py-24 bg-cream-50" aria-labelledby="getting-started-heading">
			<div class="container mx-auto px-6">
				<div class="text-center mb-12">
					<h2 id="getting-started-heading" class="text-3xl md:text-4xl font-display font-light text-charcoal tracking-wide">Getting Started Is Easy!</h2>
				</div>
				<div class="grid md:grid-cols-3 gap-10 md:gap-14 max-w-7xl mx-auto mb-12">
					{steps.map((step) => <StepCard {...step} />)}
				</div>
				<div class="text-center">
					<Button href="#get-started" variant="primary" size="large">
						3 Week Intro for $56
					</Button>
				</div>
			</div>
		</section>

		<!-- Core Pillars Section -->
		<section class="py-24 bg-white" aria-labelledby="pillars-heading">
			<div class="container mx-auto px-6">
				<div class="text-center mb-16">
					<p class="text-neutral-500 text-sm font-medium tracking-luxury-wide uppercase">Our Foundation</p>
					<h2 id="pillars-heading" class="text-3xl md:text-4xl font-display font-light text-charcoal mt-4 mb-6 tracking-wide">Three Pillars</h2>
					<Divider width="short" />
				</div>
				<div class="grid md:grid-cols-3 gap-8">
					{pillars.map((pillar) => <PillarCard {...pillar} />)}
				</div>
			</div>
		</section>

		<!-- Quote Section -->
		<section class="py-20 bg-cream-100">
			<div class="container mx-auto px-8 max-w-3xl text-center">
				<blockquote class="text-2xl md:text-3xl font-display font-light text-charcoal leading-relaxed">
					"Where natural beauty meets intentional practice"
				</blockquote>
			</div>
		</section>

		<!-- About Section -->
		<section class="py-24 bg-cream-100" aria-labelledby="about-heading">
			<div class="container mx-auto px-6">
				<div class="grid md:grid-cols-2 gap-20 items-center max-w-6xl mx-auto">
					<div>
						<img src="https://images.unsplash.com/photo-1599901860904-17e6ed7083a0?w=800&h=1000&fit=crop&q=80" alt="Yoga studio atmosphere" loading="lazy" class="rounded-2xl shadow-md border border-warm-gray-200" />
					</div>

					<div>
						<p class="text-neutral-500 text-sm font-medium tracking-luxury-wide uppercase">Our Story</p>
						<h2 id="about-heading" class="text-3xl md:text-4xl font-display font-light text-charcoal mt-4 mb-6 tracking-wide">Who is Love More?</h2>
						<Divider width="short" class="mb-8" />
						<p class="text-lg text-neutral-600 mb-6 leading-relaxed">
							We're a refined community dedicated to guiding you on a transformative self-love journey through movement, community, and connection.
						</p>
						<p class="text-base text-neutral-600 mb-10 leading-relaxed">
							Through our curated classes, intimate events, specialized trainings, transformative retreats, and healing services, our mission is to embrace the art of learning to LOVE MORE.
						</p>
						<Button href="/about" variant="primary">
							Discover Our Story
						</Button>
					</div>
				</div>
			</div>
		</section>

		<!-- Virtual Studio Banner -->
		<section class="py-20 bg-white" aria-labelledby="virtual-heading">
			<div class="container mx-auto px-8 max-w-4xl">
				<div class="bg-cream-200 rounded-2xl p-12 text-center">
					<Calendar class="w-12 h-12 text-brand mx-auto mb-4" aria-hidden="true" />
					<h2 id="virtual-heading" class="text-3xl md:text-4xl font-display font-light text-charcoal mb-4">
						Join Our Virtual Studio
					</h2>
					<p class="text-lg text-neutral-600 mb-8">
						Practice with us from anywhere with our curated online classes
					</p>
					<Button href="/virtual" variant="primary">
						Explore Virtual Classes
					</Button>
				</div>
			</div>
		</section>

		<!-- Email Signup Section -->
		<section class="py-24 bg-cream-50" aria-labelledby="newsletter-heading">
			<div class="container mx-auto px-6">
				<div class="max-w-3xl mx-auto">
					<div class="bg-white p-12 rounded-2xl shadow-md border border-warm-gray-200">
						<div class="text-center mb-10">
							<Mail class="w-12 h-12 text-brand mx-auto mb-6" aria-hidden="true" />
							<h2 id="newsletter-heading" class="text-3xl md:text-4xl font-display font-light text-charcoal mb-4 tracking-wide">Let's Stay Connected</h2>
							<p class="text-lg text-neutral-600">Join our refined community for exclusive updates, curated events, and inspiration.</p>
						</div>

						<form name="newsletter" method="POST" action="/thank-you" data-netlify="true" netlify-honeypot="bot-field" class="space-y-6">
							<input type="hidden" name="form-name" value="newsletter" />
							<p class="hidden" aria-hidden="true"><label>Don't fill this out if you're human: <input name="bot-field" tabindex="-1" autocomplete="off" /></label></p>
							<div>
								<label for="email" class="sr-only">Email address</label>
								<input type="email" id="email" name="email" placeholder="Your email address" required aria-required="true" class="w-full px-8 py-5 rounded-lg border border-warm-gray-200 focus:border-brand focus:ring-4 focus:ring-brand/20 outline-none transition-all text-lg" />
							</div>
							<Button type="submit" class="w-full">
								Join Our Community
							</Button>
						</form>

						<p class="text-sm text-neutral-500 text-center mt-8">We respect your privacy. We will never share your email or spam you.</p>
					</div>
				</div>
			</div>
		</section>
	</main>

	<Footer />
</Layout>
